export declare const VISUALIZER_IMPORT_MAP: {
  imports: Record<string, string>;
};

/** Canonical inner text of the `<script type="importmap">` element. The HTML
 *  injection and the CSP hash MUST both use exactly this string. */
export declare const VISUALIZER_IMPORT_MAP_JSON: string;
